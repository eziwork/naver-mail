# v0.2.1 사전 배포 — Mac 설치와 최초 연결 개선

GitHub 주소를 전달하면 AI가 일회성 설치 도우미를 실행합니다. 사용자가 Node·Rust·터미널 사용법을 배울 필요는 없습니다. [설치 지침](https://github.com/eziwork/naver-mail/blob/main/docs/INSTALL_FROM_GITHUB.md)

```text
https://github.com/eziwork/naver-mail
이 플러그인을 설치해 줘.
```

## 변경 내용

- 사전 배포판이 `/releases/latest`에서 빠지는 문제를 피하도록 공개 Release 목록을 탐색합니다.
- 별도의 Rust 설치 도우미가 패키지 선택·체크섬·파일 검사·CLI 등록과 중단 복구를 담당합니다. 설치 후 종료되며 상시 메모리를 추가하지 않습니다.
- Mac 작업 프로세스를 독립 세션으로 실행합니다. 설정은 최초 생성부터 최대 30분 유지하고 완료·취소·만료 후 정리합니다.
- 앱 비밀번호 입력 단계에 네이버 보안설정 버튼을 추가했습니다. 입력 중 서버 끊김 표시, 남은 시간, 새로고침 후 단계 복원을 제공합니다.
- 비밀번호는 브라우저 저장소에 보관하지 않습니다. 동일한 유효 세션의 이메일·진행 단계만 탭 저장소에 임시 보관하고 완료·취소·만료 시 지웁니다.
- 기존 12개 도구와 계정 저장 형식을 유지합니다. 발송 결과가 불명확할 때 자동 재발송하지 않습니다.

## 검증 상태와 한계

2026-09-08 Windows x64·Mac Intel·Apple Silicon의 CI와 사전 배포가 모두 성공했습니다. [플랫폼별 측정값과 실제 검증 범위](https://github.com/eziwork/naver-mail/blob/main/docs/measurements/v0.2.1-ci.md)를 참고하세요.

Windows 로컬에서 TypeScript 검사·40개 단위 테스트, 설치 도우미 테스트, Chromium의 입력/검사/완료 새로고침·끊김 감지·390px 화면·200% 확대를 검사했습니다. 배포 ZIP을 새 폴더에 풀어 동봉 키링 로드와 설정 프로세스 유지·재접속·정리를 검사했습니다.

세 플랫폼 CI는 Windows x64, Mac Intel, Mac Apple Silicon에서 네이티브 빌드, 설치 도우미 테스트, 브라우저 테스트, 배포 패키지 검사, 30초 유휴·메모리·시작 시간을 검사합니다. Mac에서는 부모 프로세스 그룹 종료도 테스트합니다. 결과는 [GitHub Actions](https://github.com/eziwork/naver-mail/actions)에서 확인합니다. CI 성공은 실제 Codex 앱·Safari·사용자 키체인 검증을 대신하지 않습니다.

2026-09-06 팀원 피드백에는 Mac에서 IMAP·SMTP 인증과 키체인 저장에 성공했다는 기록이 있습니다. 이는 v0.2.0의 사용자 제보이며, v0.2.1에 대해 독립 검증한 결과가 아닙니다.

**실제 팀원 Mac에서 남은 검증:** 저장소 주소부터 깨끗한 설치, 설정 중 Codex 정상 종료 후 브라우저에서 완료, 앱 재접속 후 메일 조회, Safari/Chrome 키보드 조작과 키체인 허용·거절. 실제 메일은 발송하지 않았습니다. 발송 검증은 별도 동의를 받은 테스트 메일로만 합니다.

개발자 코드서명·macOS 공증은 아직 완료되지 않았습니다. 미서명 사전 배포판이며 정식 지원 검증 완료로 표시하지 않습니다. OS의 실행 차단 시 표시되는 안내를 확인하세요.

메모리 목표는 설정·발송 확인 대기가 없는 연결 1개에서 20MiB 이하, 유휴 Node 0개, 네이버 통신을 제외한 준비 시간 3초 이하입니다. Windows에서 다른 빌드 없이 5회 재측정한 결과 대기 최대 **6.95MiB**, 준비 **525–585ms**, 실제 30초 유휴 후 **Node 0개**로 통과했습니다. 호스트 런타임 측정이며 동봉 런타임은 CI에서 별도 검사합니다. 앞선 Rust 빌드 병행 측정에서는 최초 4.874초로 목표를 넘었고 이후 4회는 0.488~0.549초였습니다. [측정 기록](https://github.com/eziwork/naver-mail/tree/main/docs/measurements)

## 배포 파일과 복구

Assets에는 세 운영체제용 플러그인 패키지, 세 설치 도우미, 각각의 `.sha256`, 전체 `SHA256SUMS.txt`가 함께 있습니다. `Source code`는 개발 원본입니다. 검증 작업이 성공하고 모든 파일이 준비된 뒤 사전 배포판을 공개합니다.

이전 v0.2.0 자산은 변경하지 않습니다. 설치 실패 시 도우미의 다음 실행에서 이전 폴더와 등록을 복구합니다. 수동 복구에서는 보관한 이전 패키지를 기존 마켓플레이스로 재설치합니다. OS 키체인 레코드는 유지합니다. 진단 기록에는 실행 시각·실행 방식·종료 종류만 남기며 계정·비밀번호·설정 URL·메일 내용은 기록하지 않습니다.

로컬 데스크톱용입니다. 웹·클라우드 Work, 컴퓨터 재부팅 후 설정 세션 복원, 상시 메일 감시는 포함하지 않습니다.
